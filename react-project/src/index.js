import React from "react";
import ReactDOM from "react-dom/client";
import Bord from "./component/Forget-verfication/Bord.js" ;
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Bord/>
  </React.StrictMode>
);
