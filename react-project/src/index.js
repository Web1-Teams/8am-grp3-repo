import React from "react";
import CardForget from "../src/components/react-fathi-border/CardForget"
import ReactDOM from "react-dom/client";
import App from "./App";
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <CardForget/>
  </React.StrictMode>
);
